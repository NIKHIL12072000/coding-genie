apiVersion: apps/v1
kind: Deployment
metadata:
  name: coding-genie-frontend
  namespace: coding-genie
spec:
  replicas: 1
  selector:
    matchLabels:
      app: coding-genie-frontend
  template:
    metadata:
      labels:
        app: coding-genie-frontend
    spec:
      containers:
        - name: coding-genie-frontend
          image: 12072000/coding-genie-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: coding-genie-frontend
  namespace: coding-genie
spec:
  type: ClusterIP
  selector:
    app: coding-genie-frontend
  ports:
    - port: 80
      targetPort: 80