# k8s/infra/core-network-policies.yaml

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-internal-only
  namespace: coding-genie
spec:
  podSelector: {} # Applies to all pods in coding-genie
  policyTypes:
    - Ingress # policy only governs incoming traffic
  ingress: #only pods in the same namespace can reach the selected pods.
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: coding-genie

---
# 2. NEW POLICY: Allow Previews to talk to MinIO (for file storage)
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-previews-to-minio
  namespace: coding-genie
spec:
  # Target ONLY the MinIO pod (adjust matchLabels if your MinIO pod label is different)
  podSelector: 
    matchLabels:
      app: minio # or statefulset.kubernetes.io/pod-name: minio-0
  policyTypes:
    - Ingress
  ingress:
    # Allow traffic FROM the coding-genie-previews namespace
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: coding-genie-previews
      # ONLY on port 9000
      ports:
        - protocol: TCP
          port: 9000

---
# 3. NEW POLICY: Allow NGINX Ingress to talk to API Gateway and Proxy
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-nginx-ingress
  namespace: coding-genie
spec:
  # This applies to the services Nginx needs to reach
  podSelector:
    matchExpressions:
      - key: app
        operator: In
        values: ["api-gateway", "coding-genie-proxy", "coding-genie-frontend"] # Add any other services that need to be exposed via Ingress
  policyTypes:
    - Ingress
  ingress:
    # Allow traffic FROM the ingress-nginx namespace
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ingress-nginx